# To create the git local  
 - git init -b main 
 - git add . 
 - git commit -m "initial commit"

# Going to github to create the Git Repository
 - git remote add origin <remote_url>
 - git branch -M main
 - git push -u origin main